# Google Identity Services (OAuth) — Web client ID from Google Cloud Console
# https://console.cloud.google.com/apis/credentials — create "Web application" OAuth client
# Authorized JavaScript origins: http://localhost:5173 (and your production URL)
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
